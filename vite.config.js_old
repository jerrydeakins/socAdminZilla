import { defineConfig } from "vite";

export default defineConfig({
  build: {
    target: "firefox120",
    minify: false,
    sourcemap: true,
    rollupOptions: {
      input: "content/vk.js",
      output: {
        entryFileNames: "vk.js",
        format: "iife",
      },
    },
    outDir: "dist/content",
    emptyOutDir: true,
  },
});