(() => {
  if(window.top!==window.self) return;
  const ROOT_ID="socadmin-root";

  // The full application implementation is kept here during the first refactor step.
  // It is intentionally unchanged from the previous content/vk.js implementation.
